# Fallback DB config (env vars take precedence)
# Recommended to set via environment variables:
#   EXPENSE_DB_URL, EXPENSE_DB_USER, EXPENSE_DB_PASS

db.url=jdbc:mysql://localhost:3306/expense_tracker?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
# Update as needed if not using env vars
db.user=root
db.password=
